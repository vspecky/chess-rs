extern crate ggez;
mod game;

use ggez::{
    event::{self, EventHandler},
    graphics, Context, ContextBuilder, GameResult,
};

const WIN_WIDTH: u32 = 800;
const WIN_HEIGHT: u32 = 800;

fn main() {
    /*
    let (mut ctx, mut event_loop) =
        ContextBuilder::new("Rust Chess", "vSpecky")
            .build()
            .unwrap();
    */
}
